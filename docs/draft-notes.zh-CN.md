# 未发布草稿 — 首发待定

项目尚未首发，由所有者确定发布时机和版本号。

[English](draft-notes.md)

本项目定位为可用社区 base 上的增量加速补丁，运行库、模型、游戏接入、分辨率及原有兼容适配继续由 base 负责。

一次安装，无需选择 1080p、1440p 或 4K。完整本地包直接沿用 DLSS5 的网络尺寸、
padding 和启动参数，已检查 16:10、超宽屏等输入。之前折叠高宽的四个位置增加
通用变体，已测尺寸的快速路径保留自动选择。Status 显示实际观察到的 NR 尺寸。

独立社区/优化进程对比三帧后最终图像，包含静态、运动及一组 NR+SR。
这是正确性检查；已有耗时表仍属于冻结的 此前固定尺寸草稿，不宣称新游戏测试或新性能百分比。
详见动态尺寸报告。

开发草稿继续保持 **Private**，协作者可使用 Issues 和 Discussions。
本地 SOURCE-PREVIEW 压缩包 包含中英文说明、管理工具源码、检查及结果，
**不含加速内核或私有实现**。完整 当前自动尺寸草稿 包留在本地，GPU 代码分发授权仍未解决。

管理器可安全移除已知插件，包括恢复旧 此前固定尺寸草稿 安装。硬件、运行库身份与模式检查继续保留；
动态尺寸不等于其他显卡、驱动、游戏、HDR 或动态分辨率模式已经验证。
